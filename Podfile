# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Class Finder' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Class Finder
  pod 'GoogleMaps'
  pod 'GooglePlaces'

  target 'Class FinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Class FinderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
